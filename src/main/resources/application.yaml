spring:
  application:
    name: kotlin-chat-test
  rabbitmq:
    host: "localhost"
    port: 5672
    username: "guest"
    password: "guest"
    template:
      retry:
        enabled: true
        initial-interval: "2s"

common:
  mode: scalable